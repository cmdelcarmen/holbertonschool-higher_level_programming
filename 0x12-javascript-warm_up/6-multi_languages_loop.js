#!/usr/bin/node

/*
 * Scipt prints 3 lines using an array
 */

const array = ['C is fun', 'Python is Cool', 'JavaScript is amazing'];
let idx = 0;
for (idx = 0; idx < 3; idx++) {
  console.log(array[idx]);
}
